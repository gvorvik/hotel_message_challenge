import React from 'react';

const Companies = (props) => {
    return(
        <div>
            <h2>Please select a company</h2>
            <select>
                <option> </option>
            </select>
        </div>
    )
}

export default Companies;